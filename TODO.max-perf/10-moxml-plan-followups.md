# Lever 10: moxml Plan follow-ups (upstream asks)

Status: Plan is merged upstream (lutaml/moxml#251, 0.5.60).
Asks for the next round:
- Promote plan_rows to a documented adapter contract + conformance
  spec (base adapter currently nil-fallbacks to the NodeSet walk).
- Port plan_rows to the other adapters.
- Add a Plan row to the moxml bench matrix (lock the
  raw-nokogiri-parity claim in CI).
